#include <stdio.h>
#include <stdlb.h>
#include <cs50.h>


//theta() is the average case.
//use hash tables if you don't care if data is sorted
//a hashtable has two main parts a hash function and
//an array that stores the data
//what makes a good hash function is
//-Use ONLY the data being hashed, don't check irrleveant data
//then use ALL of the hashed data, throughly check through the realevent data.
//-Being "deterministic", every time we put in a specfifc input we would get the same output.
//-Should have a large range of hashcodes, so even for similar data there is a large difference in
//the returned hashcodes.


int main()
{



}
